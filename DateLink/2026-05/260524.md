# 오늘 한 일

### React + TypeScript 프로젝트 제작 (React, TypeScript, ExpressJS, MongoDB로 만드는 실시간 통신 애플리케이션)

- 그룹 채팅방 툴팁 메시지 출력 테스트 (사이드바 문제 테스트)
  - 사이드바에서 드래그 시 빈 공간이 생기는 문제를 해결하기 위해 그룹 채팅방에 구성된 툴팁 메시지 내용을 수정해 테스트
  - 프론트엔드
    - GroupChat
      - tooltipPos useState 추가
        - 초기값은 객체로 구성
          - x, y는 0으로 구성
        - 타입 정의는 `{ x: number, y: number }`로 구성
      - showTooltip useState 추가
        - 초기값은 false로 구성
        - 타입은 boolean으로 구성
      - mouseEnterHandler 함수 추가
        - event를 추가하고 `React.MouseEvent<HTMLDivElement>`로 타입 정의
        - rect 변수 추가
          - `e.currentTarget.getBoundingClientRect`로 구성
          - 현재 화면상 위치와 크기 정보를 가져오는 내용
          - 클릭 시에 해당 요소의 위치/크기를 객체로 반환해 저장
        - tooltipPos를 업데이트하는 내용 추가
          - 객체로 구성
          - x는 `rect.right + 8`로 구성
          - y는 `rect.top + rect.height / 2`로 구성
        - showTooltip를 업데이트하는 내용 추가
          - setShowTooltip를 통해 true로 업데이트하도록 구성
      - mouseLeaveHandler 함수 추가
        - setShowTooltip를 통해 false로 업데이트하도록 구성
      - JSX 코드 내용 수정
        - 그룹 채팅방 관련 div 수정
          - onMouseEnter props를 추가해 mouseEnterHandler 함수로 구성
          - onMouseLeave props를 추가해 mouseLeaveHandler 함수로 구성
        - 인디케이터 관련 span 주석 해제
        - 툴팁 메시지 관련 div 수정
          - showTooltip useState를 조건부로 구성
          - style 추가
            - position을 "fixed"로 구성
            - left를 tooltipPos.x로 구성
            - top을 tooltipPos.y로 구성
            - transform을 `translateY(-50%)`로 구성
  - CSS
    - GroupChat
      - tooltip-text
        - 테스트를 위해 추가하고 주석 처리
        - 색은 var(--color-text), 배경색은 var(--group-chat-btn)로 구성
        - padding 위아래는 0.65rem, 좌우는 var(--space-sm)로 구성
        - 테두리는 var(--radius-md)로 구성
        - var(--radius-md)는 nowrap으로 구성
        - 포인터 이벤트는 none으로 구성
        - z-index는 9999로 구성
        - opacity는 1로 구성
  - 사이드바에서 그룹 채팅방 드래그 시에 우측에 빈 공간이 생기는데 해당 문제를 해결하기 위해 툴팁 메시지 관련 내용을 수정해 확인한 결과, 툴팁 메시지 내용이 기존과 동일하게 보여지며 그룹 채팅방을 드래그해도 우측에 빈 공간이 생기지 않는 것을 확인할 수 있었다.
    - 그룹 채팅방 툴팁 메시지 관련 CSS를 조금 더 수정해 필요한 내용만 남기고 중복되거나 불필요한 내용은 삭제 필요
    - 마지막으로 읽은 메시지 관련 스크롤 위치 내용 추가 확인 및 수정 필요
    - 로컬 스토리지에 저장되는 마지막으로 읽은 메시지 내용이 마지막으로 읽은 메시지 ID 정보가 저장되도록 구성하고 테스트 필요
    - 사용자 설정에서 사용될 기능 모두 구성 후 UI 수정 필요
    - 비밀번호 수정 관련 UI 수정 필요
    - 사용자 설정 내용에서 왼쪽 영역은 사이드바처럼 선택할 수 있는 항목이 보여지도록 구성해야 하며, 오른쪽은 선택한 항목과 관련된 내용이 보여지도록 구성 필요
    - 사용자 프로필, 사용자 프로필 세부 내용에 구성된 아바타와 온라인 점 관련 내용 아바타 컴포넌트로 재사용 가능한 지 확인 필요
    - 도착한 알림을 따로 로그로 확인할 수 있는 내용 추가 필요
    - 사용하지 않는 내용 또는 주석 처리되어 필요없는 내용 정리 필요
    - 프론트엔드에서 백엔드로 전달하는 데이터 중에 사용하지 않는 데이터가 있는지 확인하고, 불필요하게 전달하지 않도록 수정 필요 (그룹 채팅방, 레이아웃, UI, 사용자 등 확인 필요)
    - 로그인, 회원가입 페이지 배경에 사용되는 여러 이미지를 더 추가해 테스트해봐야 함
    - 해야 할 일에 정리된 내용 처리 필요
    - 여러 내용에 주석을 추가해 구분 필요
    - 회원 탈퇴하는 방법이 없기 때문에 해당 내용 추가 필요
  - [코드 내용](https://github.com/jeongsangtae/float-chat/commit/63fc54cced29779d41074a36885190fc0d609344)

# 오늘 느낀 점

- 사이드바에서 그룹 채팅방 드래그 시에 우측에 빈 공간이 생기는 문제가 발생하는데 이 문제가 인디케이터와 툴팁 메시지로 인해 발생하는 것으로 알고 있었으나 인디케이터는 상관이 없고 툴팁 메시지만의 문제였기 때문에 해당 내용을 수정해 테스트해본 결과, 그룹 채팅방을 드래그해도 우측에 빈 공간이 생기지 않는 것을 확인했고 그룹 채팅방에 커서를 올리면 툴팁 메시지도 문제없이 보여지는 것을 모두 확인했다. 툴팁 메시지 관련 CSS를 추가로 수정해 불필요한 내용은 삭제하고 필요한 내용만 구성할 예정이다. 드래그 시 빈 공간이 생기는 문제를 처리하는데 좀 시간이 걸렸지만 얼추 해결이 되었기 때문에 이제 마지막으로 읽은 메시지 관련 내용 또는 사용자 설정 화면 UI를 수정해줄 생각이다.

<br />

# 내일 할 일

- 여러 내용에서 중복된 데이터 전송이나 사용하지 않는 데이터를 전송하고 있는지 확인하고 수정해야 함

- 친구 관련된 내용에서 코드 정리 필요 (코드를 깔끔하게 보이도록 수정 필요)

- 여러 Zustand에 구성된 실시간 반영 로직들이 재사용될 수 있도록 커스텀 훅에 옮겨서 구성하는 것을 고려해야 함

<br />

# 앞으로 해야 할 일

- React, TypeScript 공부

- React 상태 관리 도구인 Zustand 공부

- 실시간 통신 애플리케이션 프로젝트 제작
