# 오늘 한 일

### React + TypeScript 프로젝트 제작 (React, TypeScript, ExpressJS, MongoDB로 만드는 실시간 통신 애플리케이션)

- 계정 삭제 폼 이름 수정 및 계정 삭제 관련 내용 추가, 수정
  - 계정 삭제 폼 이름을 약간 수정해 다른 컴포넌트 이름간의 통일성을 지키고 계정 삭제 관련 입력창과 함수 그리고 액션 함수 등 여러 내용을 추가 및 수정해 테스트
  - 프론트엔드
    - authStore
      - AuthStore 타입 정의 수정
        - deleteUserForm 액션 함수 타입 정의 추가
          - 매개변수로 payload를 받도록 구성하고 DeleteUserData로 타입 정의
          - 비동기 함수로 구성하고 AuthResult로 타입 정의
      - deleteUserForm 액션 함수 추가
        - payload 내용을 객체 디스트럭처링해 password, confirmText로 구성
        - requestBody 객체 변수 추가
          - password, confirmText로 구성
        - fetch 함수 추가
          - "/deleteUserForm" 경로로 구성
          - method는 "DELETE"로 구성
          - body를 통해 requestBody 전달
          - headers, credentials는 기본 구성
        - 응답이 올바른지 확인하는 조건부 추가
          - errorData 변수 추가
            - 응답 관련 내용을 전달받도록 구성
          - success는 false, message는 errorData.message를 반환하도록 구성
        - 올바른 응답 시에 success를 true로 반환하도록 구성
        - 예외 처리에서 success는 false, type은 "NETWORK_ERROR"를 반환하도록 구성
      - EditUserPasswordPayload, EditUserPasswordRequestBody 타입 정의 삭제
      - editUserPasswordForm 액션 함수 수정
        - requestBody 객체 변수 타입을 EditUserPasswordData로 수정
    - Login
      - 회원가입 시 실행되는 useEffect 수정
        - if문에 else if문을 추가해 계정 삭제와 관련된 조건부 추가
          - `location.state?.userDeleteSuccess`를 확인하도록 구성
          - 계정 탈퇴시 toast.success를 통해 "계정 탈퇴 완료"가 출력되도록 구성
          - `navigate(location.pathname, { replace: true })`를 추가해 내용 정리
    - UserSettings
      - DeleteAccountForm 컴포넌트 대신 DeleteUserForm 가 사용되도록 수정
      - activeView useState 수정
        - 타입 정의에 구성된 "deleteAccount"를 "deleteUser"로 수정
      - JSX 코드 내용 수정
        - DeleteAccountForm를 모두 DeleteUserForm로 수정하고 deleteAccount 또한 deleteUser로 수정
      - activeView useState 수정
        - 타입 정의에 "deleteAccount" 항목 추가
      - JSX 코드 내용 수정
        - 계정 탈퇴 버튼 추가
          - onClick을 추가하고 setActiveView를 통해 "deleteAccount"로 업데이트되도록 구성
        - 계정 탈퇴 화면으로 전환되는 내용 추가
          - activeView와 "deleteAccount"가 일치하는지 확인하는 조건부 추가
          - 조건부 만족 시에 DeleteAccountForm 컴포넌트 출력
          - DeleteAccountForm 컴포넌트에 onBack props 전달하고 setActiveView를 통해 "auth"로 업데이트되도록 구성
    - DeleteUserForm
      - useNavigate를 불러와 navigate로 사용할 수 있도록 구성
      - useAuthStore Zustand에서 deleteUserForm 액션을 불러와 사용할 수 있도록 구성
      - initialUserDeleteData 객체 변수 추가
        - password, confirmText를 추가해 string으로 구성
      - userDeleteData useState 추가
        - DeleteUserData를 불러와 타입 정의
        - 초기값은 initialUserDeleteData로 구성
      - showPassword useState 추가
        - 초기값은 boolean으로 구성
      - errorMessage useState 추가
        - string으로 타입 정의하고 초기값은 빈 문자열로 구성
      - inputChangeHandler 함수 추가
        - event는 `React.ChangeEvent<HTMLInputElement>`로 타입 정의
        - event.target을 객체 디스트럭처링해 name, value로 구성
        - setUserDeleteData를 통해 기존 내용은 `...userDeleteData`로 유지되고 새롭게 입력한 내용은 키-값을 통해 업데이트되도록 구성
      - submitHandler 함수 수정
        - userDeleteResult 변수 추가
          - deleteUserForm 액션에 userDeleteData를 전달하고 반환하는 내용을 저장하도록 구성
        - 반환받은 내용을 확인하는 조건부 추가
          - `!userDeleteResult.success`와 `userDeleteResult.type === "NETWORK_ERROR"`를 확인해 일치하면 toast.error를 통해 "네트워크 문제 발생" 메시지를 출력하고 종료하도록 구성
          - `!userDeleteResult.success`를 확인해 일치하면 setErrorMessage를 통해 userDeleteResult.message 또는 "에러 발생" 메시지로 업데이트되며 종료하도록 구성
        - 계정 탈퇴 성공 시 실행되는 내용 추가
          - setErrorMessage를 통해 빈 문자열로 업데이트되도록 구성
          - setUserDeleteData를 통해 initialUserDeleteData로 업데이트되도록 구성
          - navigate를 실행해 "/login"로 이동하며 `state: { userDeleteSuccess: true`를 전달하도록 구성
      - JSX 코드 내용 수정
        - 비밀번호 입력란 수정
          - value를 userDeleteData.password로 구성
          - onChange를 inputChangeHandler로 구성
        - 비밀번호 입력란에 눈 아이콘 추가
          - type은 button으로 구성
          - onClick을 추가하고 setShowPassword를 통해 반대로 전환되도록 구성
          - showPassword를 확인해 true면 EyeOff 아이콘, false면 Eye 아이콘이 보여지도록 구성
        - 확인 문구 수정
          - value를 userDeleteData.confirmText로 구성
          - onChange를 inputChangeHandler로 구성
        - 에러 메시지 관련 내용 추가
          - error-message className으로 구성
          - errorMessage 내용으로 구성
    - types
      - DeleteUserData 타입 정의 추가
        - password, confirmText 모두 string으로 타입 정의
  - CSS
    - DeleteUserForm
      - error-message
        - height는 0.1rem으로 구성
        - line-height는 0.5rem으로 구성
        - 색은 var(--color-error)로 구성
        - 폰트 크기는 var(--font-xs)로 구성
        - opacity는 1로 구성
  - 다른 컴포넌트 이름처럼 통일성을 가지도록 계정 삭제 폼 이름을 수정하고 계정 삭제 관련 입력창과 액션 함수 등 여러 내용을 추가하고 제출 함수나 JSX 코드 내용을 수정해 확인한 결과, 계정 탈퇴를 클릭하면 로그인 페이지로 이동하는 것은 확인했지만 아직 로그인 정보를 초기화하는 내용이 없어서 바로 재로그인이 되기 때문에 관련 내용을 더 추가한 뒤 추가 테스트가 필요하다.
    - 계정 탈퇴 관련 로직을 구성해 테스트 진행 필요
    - 사용자 설정에서 사용될 기능 모두 구성 후 UI 수정 필요
    - 비밀번호 수정 관련 UI 수정 필요
    - 사용자 설정 내용에서 왼쪽 영역은 사이드바처럼 선택할 수 있는 항목이 보여지도록 구성해야 하며, 오른쪽은 선택한 항목과 관련된 내용이 보여지도록 구성 필요
    - 비밀번호 변경과 관련된 액션 함수 구성 필요
    - 사용자 프로필, 사용자 프로필 세부 내용에 구성된 아바타와 온라인 점 관련 내용 아바타 컴포넌트로 재사용 가능한 지 확인 필요
    - 도착한 알림을 따로 로그로 확인할 수 있는 내용 추가 필요
    - 사용하지 않는 내용 또는 주석 처리되어 필요없는 내용 정리 필요
    - 프론트엔드에서 백엔드로 전달하는 데이터 중에 사용하지 않는 데이터가 있는지 확인하고, 불필요하게 전달하지 않도록 수정 필요 (그룹 채팅방, 레이아웃, UI, 사용자 등 확인 필요)
    - 로그인, 회원가입 페이지 배경에 사용되는 여러 이미지를 더 추가해 테스트해봐야 함
    - 해야 할 일에 정리된 내용 처리 필요
    - 여러 내용에 주석을 추가해 구분 필요
    - 회원 탈퇴하는 방법이 없기 때문에 해당 내용 추가 필요
  - [코드 내용](https://github.com/jeongsangtae/float-chat/commit/439ce2bbbf3b73c964062559fa7276f0f0fd4e97)

# 오늘 느낀 점

- 컴포넌트 이름이 통일성을 가지도록 계정 삭제 폼 이름을 수정하고 계정 삭제 관련 입력창, 관련 액션 함수를 추가하고 제출 함수나 JSX 코드를 수정한 뒤 테스트한 결과, 계정 탈퇴 버튼 클릭 시에 로그인 페이지로 이동하고 다시 남아있는 로그인 정보로 인해 재로그인이 되고 있는데 여기에 백엔드 관련 로직을 추가해 인증 정보를 초기화하도록 구성하면 해당 문제는 해결될 것으로 생각이 된다. 여기에 계정 탈퇴 시에 그룹 채팅방 삭제나 다이렉트 채팅방 삭제 그리고 친구 목록 모두 삭제되도록 구성을 해야 하는데, 이 부분은 많이 고민해본 결과 전체적으로 모두 삭제되도록 하는 것이 깔끔하다고 생각이 되며 관련 정보가 모두 삭제되는 방향으로 진행할 생각이다.

<br />

# 내일 할 일

- 여러 내용에서 중복된 데이터 전송이나 사용하지 않는 데이터를 전송하고 있는지 확인하고 수정해야 함

- 친구 관련된 내용에서 코드 정리 필요 (코드를 깔끔하게 보이도록 수정 필요)

- 여러 Zustand에 구성된 실시간 반영 로직들이 재사용될 수 있도록 커스텀 훅에 옮겨서 구성하는 것을 고려해야 함

<br />

# 앞으로 해야 할 일

- React, TypeScript 공부

- React 상태 관리 도구인 Zustand 공부

- 실시간 통신 애플리케이션 프로젝트 제작
