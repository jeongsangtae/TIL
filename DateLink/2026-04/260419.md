# 오늘 한 일

### React + TypeScript 프로젝트 제작 (React, TypeScript, ExpressJS, MongoDB로 만드는 실시간 통신 애플리케이션)

- 로그인 관련 fetch 함수, 제출 함수 수정 및 toast 라이브러리 추가, 테스트
  - toast 라이브러리를 추가해 레이아웃 컴포넌트에서 전역적으로 사용될 수 있도록 구성하고 로그인 관련 fetch 함수와 제출 함수에 해당 내용을 불러와 구성 및 추가로 채팅 관련 내용에 구성해 테스트
  - 프론트엔드
    - authStore
      - AuthResult 타입 정의 수정
        - type 항목을 추가하고 옵셔널 타입으로 구성하고 string으로 타입 정의
      - login 액션 함수 수정
        - catch문 에러 처리 수정
          - type 항목을 추가하고 "NETWORK_ERROR"라는 내용으로 반환되도록 구성
    - chatStore
      - sendMessage 액션 함수 수정
        - 메시지 전송 성공 영역 수정
          - success는 true, type은 "SUCCESS"로 반환하도록 구성
        - catch문 에러 처리 수정
          - success는 false, type은 "NETWORK_ERROR"로 반환하도록 구성
          - alert 내용은 주석 처리
    - Layout
      - react-hot-toast 라이브러리를 통해 Toaster를 불러오도록 구성
      - react-toastify 라이브러리를 통해 ToastContainer를 불러오도록 구성
      - react-toastify 라이브러리 관련 CSS 내용을 불러오도록 구성
        - 현재는 주석 처리
      - JSX 코드 내용 수정
        - Toaster 추가
          - position을 추가하고 "top-right"로 구성
        - ToastContainer 추가
          - position을 추가하고 "top-right"로 구성
          - autoClose을 추가하고 2000으로 구성
          - hideProgressBar, closeOnClick, pauseOnHover, draggable 추가
    - Login
      - react-hot-toast 라이브러리를 통해 toast를 불러오도록 구성
      - submitHandler 함수 수정
        - 로그인 성공 여부를 확인하는 조건부 수정
          - 로그인 성공 여부를 먼저 확인하고 로그인 결과 타입을 확인해 "NETWORK_ERROR"와 일치하는지 확인하도록 수정
          - `toast.error`를 추가해 "네트워크 문제 발생" 텍스트가 출력되도록 구성
    - ChatInput
      - react-hot-toast 라이브러리를 통해 toast를 불러오도록 구성
      - react-toastify 라이브러리를 통해 toast를 불러오도록 구성
      - sendMessageHandler 함수 수정
        - sendMessageResult 변수 추가
          - sendMessage에 전달하는 내용은 그대로 유지
          - sendMessage에서 반환하는 내용을 전달받아 저장하도록 구성
        - sendMessageResult와 sendMessageResult.success를 확인하는 콘솔 로그 추가
        - 메시지 성공 여부와 메시지 전송 결과 타입이 "SUCCESS"인지 확인하는 조건부 추가
          - 메시지 전송 성공이 콘솔에 보여지도록 구성
          - toast.error를 사용해 "메시지 전송 성공" 메시지가 보여지도록 구성
            - success 테스트 후 error도 보여지는지 확인을 위해 수정 후 테스트
  - toast 라이브러리를 추가하고 레이아웃 컴포넌트에서 전역적으로 사용될 수 있도록 내용을 구성한 뒤 로그인 관련 내용과 채팅 관련 내용에 해당 내용을 불러와 구성해 확인한 결과, 추가한 두 라이브러리 모두 문제없이 실행되는 것을 확인할 수 있었다.
    - 두 가지 toast 라이브러리 추가해 테스트한 결과, react-toastify 라이브러리가 커스터마이징이 가능하기 때문에 해당 라이브러리를 사용해 추가 테스트 예정
    - 사용자 프로필, 사용자 프로필 세부 내용에 구성된 아바타와 온라인 점 관련 내용 아바타 컴포넌트로 재사용 가능한 지 확인 필요
    - 도착한 알림을 따로 로그로 확인할 수 있는 내용 추가 필요
    - 사용하지 않는 내용 또는 주석 처리되어 필요없는 내용 정리 필요
    - 프론트엔드에서 백엔드로 전달하는 데이터 중에 사용하지 않는 데이터가 있는지 확인하고, 불필요하게 전달하지 않도록 수정 필요 (그룹 채팅방, 레이아웃, UI, 사용자 등 확인 필요)
    - 로그인, 회원가입 페이지 배경에 사용되는 여러 이미지를 더 추가해 테스트해봐야 함
    - 해야 할 일에 정리된 내용 처리 필요
    - 여러 내용에 주석을 추가해 구분 필요
    - 예외 처리하는 부분에 관련해 확인 필요 (alert로 여러개의 창이 뜨면서 불편함이 있음)
    - alert 대신 toast 라이브러리 또는 알림창으로 에러 관련 내용이 뜨도록 수정 필요
  - [코드 내용](https://github.com/jeongsangtae/float-chat/commit/6d4337a9435c81a1f000591b5dab0ffa7bb52604)

# 오늘 느낀 점

- toast 라이브러리 두 가지를 추가해 로그인 관련 내용과 채팅 관련 내용에 구성한 뒤 테스트해본 결과, 둘 다 문제없이 작동하긴 하지만 하나는 가볍고 심플하지만 커스터마이징이 제한적이라서 커스터마이징이 가능한 라이브러리를 사용해 여러 내용에 구성된 alert 내용 대신 사용할 생각이다. 하지만 저 라이브러리를 구성해 사용하려면 각 액션 함수마다 모두 반환되는 내용이 있도록 구성해야 하는데 이런 방식으로 구현하는게 맞는지 좀 의문이 들어서 추가 확인이 필요할 것 같고, 좀 더 효율적으로 구성할 수 있는 방법이 있는지 확인해 테스트해볼 생각이다.

<br />

# 내일 할 일

- 여러 내용에서 중복된 데이터 전송이나 사용하지 않는 데이터를 전송하고 있는지 확인하고 수정해야 함

- 친구 관련된 내용에서 코드 정리 필요 (코드를 깔끔하게 보이도록 수정 필요)

- 여러 Zustand에 구성된 실시간 반영 로직들이 재사용될 수 있도록 커스텀 훅에 옮겨서 구성하는 것을 고려해야 함

<br />

# 앞으로 해야 할 일

- React, TypeScript 공부

- React 상태 관리 도구인 Zustand 공부

- 실시간 통신 애플리케이션 프로젝트 제작
