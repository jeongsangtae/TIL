# 오늘 한 일

### React + TypeScript 프로젝트 제작 (React, TypeScript, ExpressJS, MongoDB로 만드는 실시간 통신 애플리케이션)

- 인증, 채팅, 다이렉트 채팅방, 레이아웃, 모달, 소켓 스토어 관련 주석 수정 및 다이렉트 채팅방, 소켓 스토어 재사용 로직 추가
  - 인증, 채팅, 다이렉트 채팅방, 레이아웃, 모달, 소켓 스토어 내용을 확인해 주석 추가와 수정이 필요한 부분은 정리하고 다이렉트 채팅방과 소켓 스토어 내용에서 중복된 내용을 재사용할 수 있도록 구성
  - 프론트엔드
    - authStore, chatStore, directChatStore, layoutStore, modalStore, socketStore
      - 여러 액션 함수에 목적을 위한 주석을 추가
      - 주석이 없는 부분 추가
      - 주석이 가독성이 좀 더 좋도록 수정
      - 설명이 부족한 주석 추가 수정
      - 주석 처리되어 사용하지 않는 내용 삭제
    - directChatStore
      - sortDirectChats 함수 추가
        - getDirectChat 액션 함수에 구성된 sortFn 함수를 가져와 구성
        - a, b 매개변수를 전달받고 비교해 메시지 기준으로 채팅방 정렬하는 내용
        - 해당 내용을 getDirectChat 액션 함수와 directChatForm 액션 함수에서 사용
    - socketStore
      - addNotification 함수 추가
        - connect 액션 함수에 구성된 알림 수신 이벤트 관련 내용 가져와 구성
        - notificationData를 매개변수로 전달받도록 구성
          - NotificationData로 타입 정의
        - set 관련 내용은 이전에 구성한 내용과 동일
        - setTimeout 내용 또한 이전에 구성한 내용과 동일
          - id를 확인하는 부분에서만 notificationData 매개변수 내용을 사용해 id 확인
      - connect 액션 함수 수정
        - friendRequest, messageNotification, groupChatInviteNotification 소켓 이벤트에 addNotification 함수를 추가하고 notificationData 데이터가 사용되도록 구성
        - 이전에 구성한 set 관련 내용과 setTimeout 내용 삭제
  - 인증, 채팅, 다이렉트 채팅방, 레이아웃, 모달, 소켓 스토어 내용에 구성된 액션 함수에 주석을 추가하고 설명이 부족한 부분은 내용을 추가 그리고 수정이 필요한 주석은 수정하고 다이렉트 채팅방과 소켓 스토어 내용에서 재사용할 수 있는 내용은 따로 함수로 빼낸 뒤 재사용할 수 있도록 구성해 확인한 결과, 여러 액션 함수 내용이 어떤 역할을 하는지 이전보다 보기 편해졌고 재사용 함수로 인해 코드가 많이 줄어든 것을 확인할 수 있었다.
    - 프론트엔드에서 백엔드로 전달하는 데이터 중에 사용하지 않는 데이터가 있는지 확인하고, 불필요하게 전달하지 않도록 수정 필요
    - UI 관련 어긋나는 부분이나 의도와 다르게 동작하는 내용이 있는지 확인 필요
    - 프론트엔드 전체적인 내용을 확인해 주석 추가 및 수정 필요 (진행중)
    - 로그인, 회원가입 페이지 배경에 사용되는 여러 이미지를 더 추가해 테스트해봐야 함
    - 해야 할 일에 정리된 내용 처리 필요
    - 회원가입 시에 그냥 사용자가 이메일을 임의로 만드는 것이 아닌, 실제 존재하는 이메일로 인증받고 그 이메일에서 인증 번호를 확인해 인증하고 만드는 구조를 고려중 (해당 내용은 추후에 구성할 예정)
    - 여러 내용에 주석을 추가해 구분 필요 (백엔드 완, 프론트엔드 진행중)
  - [코드 내용](https://github.com/jeongsangtae/float-chat/commit/954b042128f0462fc5dbeb7b5d2e62d5eb990441)

# 오늘 느낀 점

- 인증과 채팅 그리고 다이렉트 채팅방과 레이아웃 그리고 모달, 소켓 스토어 내용을 확인해 구성된 액션 함수에 주석을 추가하고 설명이 부족한 내용은 추가하고 수정이 필요한 주석은 수정해 로직 블록별로 구분이 되도록 해주었고 다이렉트 채팅방과 소켓 스토어에 구성된 내용중에 재사용할 수 있는 내용은 따로 함수로 구성한 뒤 재사용해 코드를 많이 줄여주었다. 아직 친구, 그룹 채팅방 스토어 관련 내용은 정리하지 못한 상황이기 때문에 해당 내용들 또한 주석을 정리해야 하며 주석을 추가 및 수정하면서 잘못된 로직이나 재사용 가능한 내용은 재사용하도록 수정할 예정이며 모두 정리한 뒤에는 README 파일에 구성한 내용 정리와 추후 추가할 기능 정리를 해줄 예정이다.

<br />

# 내일 할 일

- 여러 내용에서 중복된 데이터 전송이나 사용하지 않는 데이터를 전송하고 있는지 확인하고 수정해야 함

- 친구 관련된 내용에서 코드 정리 필요 (코드를 깔끔하게 보이도록 수정 필요)

- 여러 Zustand에 구성된 실시간 반영 로직들이 재사용될 수 있도록 커스텀 훅에 옮겨서 구성하는 것을 고려해야 함

<br />

# 앞으로 해야 할 일

- React, TypeScript 공부

- React 상태 관리 도구인 Zustand 공부

- 실시간 통신 애플리케이션 프로젝트 제작
